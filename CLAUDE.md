@AGENTS.md

# Project context

Read `PLAN.md` first, then `context/00-overview.md` before starting work, and `context/03-decisions.md`
before changing any design choice. See `context/README.md` for what lives where.

At the end of a working session: update `context/04-progress.md` and add a log at
`context/logs/YYYY-MM-DD-session-NN.md`. Decisions belong in
`context/03-decisions.md`, not only in chat.
